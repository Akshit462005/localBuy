<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LocalBuy - <%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body class="min-h-screen bg-gray-100">
    <nav class="bg-white shadow-lg">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <a href="/" class="text-2xl font-bold text-gray-800">LocalBuy</a>
                </div>
                <div class="flex items-center space-x-4">
                    <% if (typeof user !== 'undefined') { %>
                        <% if (user.role === 'customer') { %>
                            <a href="/dashboard/customer" class="text-gray-600 hover:text-gray-900">Dashboard</a>
                            <a href="/cart" class="text-gray-600 hover:text-gray-900">Cart</a>
                        <% } else if (user.role === 'shopkeeper') { %>
                            <a href="/dashboard/shopkeeper" class="text-gray-600 hover:text-gray-900">Dashboard</a>
                            <a href="/products/manage" class="text-gray-600 hover:text-gray-900">Manage Products</a>
                        <% } else if (user.role === 'admin') { %>
                            <a href="/dashboard/admin" class="text-gray-600 hover:text-gray-900">Admin Panel</a>
                        <% } %>
                        <a href="/api/auth/logout" class="text-gray-600 hover:text-gray-900">Logout</a>
                    <% } else { %>
                        <a href="/login" class="text-gray-600 hover:text-gray-900">Login</a>
                        <a href="/register" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">Register</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-4 py-8">
        <%- body %>
    </main>

    <footer class="bg-white shadow-lg mt-8">
        <div class="max-w-6xl mx-auto px-4 py-6">
            <p class="text-center text-gray-600">&copy; <%= new Date().getFullYear() %> LocalBuy. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>